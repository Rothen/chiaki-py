name: Build (macOS)
# on:
#     push:
#         branches: [main]
#         tags-ignore:
#             - "**"
#     pull_request:
#     workflow_dispatch:
#     workflow_call:

on:
  workflow_dispatch:

jobs:
    build:
        name: Build wheel (macosx_14_0_arm64, cp311)
        runs-on: macos-14
        env:
            PYTHON_VERSION: "3.11"
            MACOSX_DEPLOYMENT_TARGET: "14.0"

        defaults:
            run:
                shell: bash

        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0

            - name: Set up Python ${{ env.PYTHON_VERSION }}
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ env.PYTHON_VERSION }}

            - name: Install Python build/CI tooling
              run: |
                  python -m pip install --upgrade pip
                  python -m pip install --upgrade "protobuf==7.36.2" "grpcio-tools==1.84.0" build twine pybind11_stubgen "delocate>=0.11"

            - name: Install native dependencies
              run: |
                  brew update
                  brew install \
                      cmake ninja nasm pkgconf \
                      protobuf openssl@3 fmt \
                      opus json-c miniupnpc libevent speexdsp \
                      ffmpeg hidapi fftw lcms2 \
                      vulkan-headers vulkan-loader

            - name: Build SDL2 (real SDL2, not sdl2-compat)
              run: |
                  brew unlink sdl2-compat || true
                  if pkg-config --exists sdl2-compat; then
                      echo "::error::sdl2-compat is still visible to pkg-config"
                      exit 1
                  fi
                  prefix="$RUNNER_TEMP/sdl2"
                  echo "SDL2_PREFIX=$prefix" >> "$GITHUB_ENV"
                  git clone --depth 1 --branch release-2.32.10 https://github.com/libsdl-org/SDL.git "$RUNNER_TEMP/SDL2-src"
                  cmake -S "$RUNNER_TEMP/SDL2-src" -B "$RUNNER_TEMP/SDL2-build" -G Ninja \
                    -DCMAKE_BUILD_TYPE=Release \
                    -DCMAKE_OSX_ARCHITECTURES=arm64 \
                    -DCMAKE_OSX_DEPLOYMENT_TARGET="$MACOSX_DEPLOYMENT_TARGET" \
                    -DCMAKE_INSTALL_PREFIX="$prefix" \
                    -DSDL_SHARED=ON -DSDL_STATIC=OFF -DSDL_TESTS=OFF
                  cmake --build "$RUNNER_TEMP/SDL2-build"
                  cmake --install "$RUNNER_TEMP/SDL2-build"

            - name: Point compiler/linker at Homebrew
              run: |
                  echo "CPATH=$(brew --prefix)/include" >> "$GITHUB_ENV"
                  echo "LIBRARY_PATH=$(brew --prefix)/lib" >> "$GITHUB_ENV"
                  echo "PKG_CONFIG_PATH=$SDL2_PREFIX/lib/pkgconfig:$(brew --prefix openssl@3)/lib/pkgconfig:$(brew --prefix protobuf)/lib/pkgconfig" >> "$GITHUB_ENV"
                  echo "$(brew --prefix protobuf)/bin" >> "$GITHUB_PATH"

            - name: Configure
              run: |
                  # Fail here, not at runtime, if the build would pick up the
                  # sdl2-compat shim instead of the real SDL2 built above.
                  pkg-config --modversion sdl2
                  if pkg-config --exists sdl2-compat; then
                      echo "::error::sdl2-compat is visible to pkg-config; the wheel would bundle the shim"
                      exit 1
                  fi
                  cmake --fresh -S . -B build-release -G Ninja -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
                    -DCMAKE_BUILD_TYPE=Release \
                    -DCMAKE_C_COMPILER=/usr/bin/clang -DCMAKE_CXX_COMPILER=/usr/bin/clang++ \
                    -DCMAKE_OSX_ARCHITECTURES=arm64 \
                    -DCMAKE_OSX_DEPLOYMENT_TARGET="$MACOSX_DEPLOYMENT_TARGET" \
                    -DCMAKE_POSITION_INDEPENDENT_CODE=ON \
                    -DPython3_EXECUTABLE="$(which python)" -DPython_EXECUTABLE="$(which python)" -DPYTHON_EXECUTABLE="$(which python)" \
                    -DCMAKE_PREFIX_PATH="$SDL2_PREFIX;$(brew --prefix);$(brew --prefix openssl@3);$(brew --prefix protobuf)" \
                    -DCMAKE_SHARED_LINKER_FLAGS="-Wl,-rpath,$(brew --prefix)/lib -Wl,-rpath,$SDL2_PREFIX/lib" \
                    -DCMAKE_MODULE_LINKER_FLAGS="-Wl,-rpath,$(brew --prefix)/lib -Wl,-rpath,$SDL2_PREFIX/lib"

            - name: Build chiaki-py
              run: cmake --build build-release --config Release --target chiaki-py -- -j"$(sysctl -n hw.ncpu)"

            - name: Build wheel
              env:
                  _PYTHON_HOST_PLATFORM: macosx-14.0-arm64
                  ARCHFLAGS: "-arch arm64"
              run: python -m build --wheel --outdir wheelhouse-raw

            - name: Repair wheel (bundle dylibs)
              run: |
                  delocate-listdeps wheelhouse-raw/*.whl
                  delocate-wheel --require-archs arm64 -w dist -v wheelhouse-raw/*.whl

            - name: Check distributions
              run: python -m twine check dist/*

            - name: Smoke test wheel
              working-directory: ${{ runner.temp }}
              timeout-minutes: 5
              run: |
                  python -m venv smoke-venv
                  smoke-venv/bin/python -m pip install --no-deps "$GITHUB_WORKSPACE"/dist/*.whl
                  smoke-venv/bin/python -m pip install numpy pydantic

                  site="$(smoke-venv/bin/python -c 'import sysconfig; print(sysconfig.get_paths()["purelib"])')"
                  status=0
                  for f in "$site"/chiaki_py/lib/chiaki_py.*.so "$site"/chiaki_py/.dylibs/*.dylib; do
                      if otool -L "$f" | tail -n +2 | grep -E '/opt/homebrew|/usr/local'; then
                          echo "::error::$f still links against Homebrew"
                          status=1
                      fi
                  done
                  [ "$status" -eq 0 ] || exit 1

                  smoke-venv/bin/python -X faulthandler -c "
                  import faulthandler
                  faulthandler.dump_traceback_later(60, exit=True)
                  import chiaki_py.lib as lib
                  print(lib.__file__)
                  assert 'site-packages' in lib.__file__
                  "

            - uses: actions/upload-artifact@v4
              with:
                  name: dist-macos
                  path: dist/
                  if-no-files-found: error
